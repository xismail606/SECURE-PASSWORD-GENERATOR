This script is designed to run on any standard Linux environment using the Bash shell.
All features work normally even without installing lolcat or additional figlet fonts.
When these optional tools are installed, the script automatically enables enhanced visual output.
•	Optional Visual Enhancements (Not Required)
The following packages provide colored banners and extra fonts, but the script will still run normally without them:
# sudo apt install lolcat
 #sudo git clone https://github.com/xero/figlet-fonts.git
# ls
# cd figlet-fonts
# Is
# rm README.md
# sudo mv * /usr/share/figlet
# showfigfonts
•	Mandatory Requirements
•	Bash shell (default on most Linux distributions)
•	Basic command-line environment

. How to Run the Script
#cd /path/to/script
#chmod +x {script name}.sh
# ./ {script name}.sh
